import 'package:flutter/material.dart';

class InstructionImageView extends StatelessWidget {
  const InstructionImageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
